rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow authenticated read/write to ALL documents for development simplicity.
    // This covers users, workouts, nutrition, AND community_posts.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}